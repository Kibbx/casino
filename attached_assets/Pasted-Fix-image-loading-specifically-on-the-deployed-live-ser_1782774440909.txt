Fix image loading specifically on the deployed/live server. The images now work in the Replit development preview, but they do NOT appear on the live deployed site. This means the frontend code may be correct locally, but the production build/server/static asset configuration is wrong.

Symptoms:
- Replit preview/dev server shows images correctly.
- Live server/deployment shows broken image icons.
- The images exist in the repo under:
  artifacts/casino/public/
- Relevant folders/files include:
  public/images
  public/cards
  public/chips
  public/rome-slots
  public/western-slots
  public/assets/horses/sprites
  public/mob-tower-bg.png
  public/backalley-slots-bg.png
  public/babalari-coin.png
  public/card-back.png
  public/case-card.png
  public/keno-card.png

Important: Do not treat this as a general UI bug. This is a production/deployment static asset serving issue.

Tasks:
1. Compare image requests in local Replit preview vs the deployed live URL.
2. Open the deployed site and inspect browser Network logs for failed image requests.
3. List the exact failed image URLs from production and map each one to the intended file in `artifacts/casino/public`.
4. Determine why production cannot serve those files even though development can.

Check these likely causes:
- The production server is not serving `artifacts/casino/public` as a static directory.
- The production build only includes `dist`, but the public assets are not being copied into `dist`.
- The deployment root differs from the dev root, so paths resolve locally but break after deploy.
- Asset paths are relative when they should be absolute from the site root.
- URLs incorrectly include `/public/...`.
- Files exist locally in Replit but were not committed/pushed/deployed to the live server.
- `.gitignore` or deployment ignore rules are excluding PNG/WebP/SVG files.
- Production references `.webp` while the actual deployed file is `.png`, or vice versa.
- Linux case sensitivity is causing filename mismatches.
- The deployed server has stale cached assets or stale build output.
- Express/Vite/static config differs between dev and production.

Fix requirements:
- Make the live deployed site serve all casino assets correctly.
- Do not redesign the UI.
- Do not replace assets with generated images.
- Do not hide broken images with CSS.
- Do not hardcode Replit preview URLs or localhost URLs.
- Do not use external image URLs.
- Keep asset references portable so they work in both Replit preview and live production.

If this is an Express server, verify production has static middleware pointing to the actual casino public directory, for example:
`app.use(express.static(path.join(process.cwd(), "artifacts/casino/public")))`

If this is Vite/React, verify either:
- the app is built from the correct casino directory where `public` is recognized, or
- assets from `artifacts/casino/public` are copied into the deployed build output.

If needed, add a production-safe build step that copies:
`artifacts/casino/public/*`
into the deployed static output directory.

Deliverables:
- Fix the deployed live server so images render in production.
- Verify the live production URL has no image 404s.
- Summarize:
  1. why it worked in Replit preview,
  2. why it failed on the live server,
  3. which files/configs were changed,
  4. how production asset serving now works.